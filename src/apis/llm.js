import apiClient from "./index";

/**
 * LLM文本对话
 * @param {Object} data - 请求参数
 * @param {string} data.message - 提示词
 * @param {string} data.model - 模型
 * @param {string} data.stream - 是否流式
 * @returns {Promise} 请求结果
 */
const chat = data => {};

export default {
  chat,
};
